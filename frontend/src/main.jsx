import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

// TODO: import App component once implemented
// import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <div>
      <h1>Urban Art Map</h1>
      <p>Application scaffold — replace with App component.</p>
    </div>
  </React.StrictMode>
);
