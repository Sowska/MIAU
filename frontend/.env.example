# Backend API base URL (include the /api path)
# Local development:
VITE_API_BASE_URL=http://localhost:5000/api

# Production (Render) — replace with your actual backend URL:
 VITE_API_BASE_URL=https://miau-backend.onrender.com/api
